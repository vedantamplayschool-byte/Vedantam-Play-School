import {Router} from 'express';import {createContact,deleteContact,getContact,listContacts,updateContact} from '../controllers/contactController.js';import {protect} from '../middleware/auth.js';import {validate} from '../middleware/validate.js';import {contactRules,idParam,pagination} from '../validators/common.js';
const r=Router();r.post('/',contactRules,validate,createContact);r.use(protect);r.get('/',pagination,validate,listContacts);r.get('/:id',idParam,validate,getContact);r.patch('/:id',idParam,validate,updateContact);r.delete('/:id',idParam,validate,deleteContact);export default r;
