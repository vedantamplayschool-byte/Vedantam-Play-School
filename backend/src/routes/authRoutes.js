import {Router} from 'express';import {body} from 'express-validator';import {login,logout,me,registerFirstAdmin} from '../controllers/authController.js';import {protect} from '../middleware/auth.js';import {validate} from '../middleware/validate.js';
const r=Router();r.post('/bootstrap',[body('name').trim().notEmpty(),body('email').isEmail().normalizeEmail(),body('password').isLength({min:8})],validate,registerFirstAdmin);r.post('/login',[body('email').isEmail().normalizeEmail(),body('password').notEmpty()],validate,login);r.post('/logout',logout);r.get('/me',protect,me);export default r;
